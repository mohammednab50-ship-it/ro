{
  "name": "rounds",
  "version": "1.0.0",
  "private": true,
  "description": "Rounds — clinical evidence app, packaged for Android via Capacitor.",
  "scripts": {
    "sync": "cap sync android",
    "build-apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/browser": "^6.0.6",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
