{
  "name": "functions",
  "description": "Cloud Functions for wards-a4e41 -- scheduled Firestore backups and Stripe subscription billing",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "true",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.9.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1",
    "stripe": "^16.12.0"
  }
}
